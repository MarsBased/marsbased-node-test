{
  "name": "node-test",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "docker:install": "docker-compose run --rm app npm install",
    "docker:run": "docker-compose run --rm app",
    "start": "docker-compose up app",
    "stop": "docker-compose stop",
    "prisma": "docker-compose run --rm app npx prisma"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@prisma/client": "^4.5.0",
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.2",
    "node-dev": "^7.4.3",
    "prisma": "^4.5.0",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
